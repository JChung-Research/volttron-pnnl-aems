#!/usr/bin/env bash
python ./scripts/install-agent.py \
    -s app_agents/RecvAgent \
    -i rec_Building5000 \
    --tag rec_Building5000 \
    --config config/recv_config_Building5000 \
    --start \
    --force
