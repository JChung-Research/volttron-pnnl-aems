#!/usr/bin/env bash
# Build a temp file string for use with the configuration
python ./scripts/install-agent.py \
    -s app_agents/RecvAgent \
    -i rec_modbus \
    --tag rec_modbus \
    --config config/recv_config_modbus \
    --start \
    --force
