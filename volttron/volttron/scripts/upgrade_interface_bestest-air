#!/usr/bin/env bash
# Build a temp file string for use with the configuration
python ./scripts/install-agent.py \
    -s app_agents/InterfaceAgent \
    -i interface_bestest_air \
    --tag interface_bestest_air \
    --config config/interface_config_bestest-air \
    --start \
    --force
